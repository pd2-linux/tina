config SUNXI_BOOT_PARTSIZE
	int "Boot filesystem partition size (in MB)"
	depends on TARGET_sunxi
	default 16

config SUNXI_RECOVERY_INITRAMFS_PARTSIZE
	int "Boot-Recovery initramfs filesystem partition size (in MB)"
	depends on TARGET_sunxi
	default 7
